import type { ReactNode } from 'react';
import type { IRegsiterProps } from './types';

export const RegisterPage = (props: IRegsiterProps): ReactNode => {
	return <div {...props}>RegisterPage</div>;
};
